/** Default support conversation id for the single-thread MVP chat. */
export const DEFAULT_SUPPORT_CONVERSATION_ID = 'default-support';

/** Recommended Gemini model for supportive chat on the free tier. */
export const RECOMMENDED_GEMINI_MODEL = 'gemini-2.5-flash';
